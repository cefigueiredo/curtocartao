<%= form_for(@movimentacao) do |f| %>
  <% if @movimentacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movimentacao.errors.count, "error") %> prohibited this movimentacao from being saved:</h2>

      <ul>
      <% @movimentacao.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="field">
    <%= f.label :grupo_id %><br />
    <%= f.select :grupo_id , Grupo.all.collect {|grupo| [grupo.nome, grupo.id]}%>
  </div>
  <div class="field">
    <%= f.label :valor %><br />
    <%= f.text_field :valor %>
  </div>
  <div class="field">
    <%= f.label :data %><br />
    <%= f.date_select :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
